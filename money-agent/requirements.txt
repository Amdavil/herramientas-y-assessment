# Núcleo
anthropic>=0.40.0          # Estrategia y estructuración con Claude (opcional; hay fallback Groq/Gemini/offline)
pandas>=2.2.0             # La "cuenta" (ledger) en Excel/CSV
openpyxl>=3.1.0          # Lectura/escritura de Excel (.xlsx)

# Búsqueda web (proveedor intercambiable)
tavily-python>=0.5.0     # Proveedor por defecto (Tavily)
requests>=2.31.0         # Proveedores Exa / Brave / Google CSE / SerpAPI y APIs de IA (Groq/Gemini)

# Utilidades
python-dotenv>=1.0.0     # Carga de variables desde .env
